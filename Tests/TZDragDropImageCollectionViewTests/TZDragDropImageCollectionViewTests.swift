import XCTest
@testable import TZDragDropImageCollectionView

final class TZDragDropImageCollectionViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TZDragDropImageCollectionView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
